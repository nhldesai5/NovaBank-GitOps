apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-owner-label
spec:
  validationFailureAction: Enforce
  background: true
  rules:
    - name: require-owner-label
      match:
        any:
          - resources:
              kinds:
                - Deployment
      validate:
        message: "Every Deployment must have an owner label."
        pattern:
          metadata:
            labels:
              owner: "?*"